/* Function somar(x, y){
    return x + y;
}*/

const somar = (x, y) => {
  return x + y;
};

let soma = somar(10, 15);
console.log(soma);

const subtrair = (x, y) => x - y;

let subtraindo = subtrair(30, 20);
console.log(subtraindo);

const sobrenome = (sob) => "José " + sob;

let lastName = sobrenome("Guilherme");
console.log(lastName);
